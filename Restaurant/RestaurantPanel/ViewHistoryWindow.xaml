<Window x:Class="RestaurantPanel.ViewHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantPanel"
        mc:Ignorable="d"
        Title="ViewHistoryWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Label Content="Filter:" VerticalAlignment="Center"/>
            <TextBox x:Name="FilterTextBox" Width="200" Margin="5"/>
            <Button Content="Apply Filter" Click="ApplyFilterButton_Click" Margin="5"/>
            <Button Content="Export CSV" Click="ExportCSVButton_Click" Margin="5"/>
        </StackPanel>

        <TextBlock x:Name="MetricsTextBlock" Grid.Row="1" Margin="10"/>

        <DataGrid x:Name="HistoryDataGrid" Grid.Row="2" AutoGenerateColumns="True" Margin="10"/>

        <Button Content="Close" Grid.Row="3" HorizontalAlignment="Right" Margin="10" Click="CloseButton_Click"/>
    </Grid>
</Window>
